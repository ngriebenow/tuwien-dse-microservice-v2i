spring:
  profiles:
    include: productive

zuul:
  routes:
    actor-registry.url: http://10.47.248.182:80

  #Ribbon is auto integrated with Zuul and for this exercise we are not using that.
ribbon:
  eureka:
    enabled: false

  #Will start the gateway server @8080
server:
  port: 8080