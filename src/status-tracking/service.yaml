apiVersion: v1
kind: Service
metadata:
  name: status-tracking-service
  labels:
    app: status-tracking-service
spec:
  selector:
    app: status-tracking-deployment
    tier: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  externalIPs:
    #- 35.246.210.242